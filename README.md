# Roguelike Game

> A simple roguelike project made using SFML and C++.

## Requirements

- CMake (Version 3.10+)
- Ninja
- Clang (C++23)
- SFML libraries (graphics, window, system)

## Build

```bash
cmake --preset` default
cmake --build --preset default
build/2d_platformer